class models(object):
    """description of class"""

#import sqlite3 as sql
#from sqlite3 import Error
#def insertUser(username,password,fullname):
#	try:
#		con = sql.connect("USER.db")
#		cur = con.cursor()
#		cur.execute("insert into users (username,password,fullname) values (?,?,?)", (username,password,fullname))
#		con.commit()
#		con.close()
#		return True
#	except:
#		return False

user = "failed"
id = 0
#def check(Username, Password):
#	con = sql.connect("USER.db")
#	cur = con.cursor()
#	cur.execute("select fullname from users where username = ? and password = ? ",(Username,Password))
#	users = cur.fetchall()
#	con.close()
#	if len(users) == 0:
#		return "failed"
#	return users[0][0]


#def comment(idPost):
#	con = sql.connect("USER.db")
#	cur = con.cursor()
#	cur.execute("select * from comment where IDPost = ? ",(idPost,))
#	cmt = cur.fetchall()
#	con.close()
#	return cmt




